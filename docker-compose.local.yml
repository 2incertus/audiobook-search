version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_API_URL=http://localhost:8000
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
    depends_on:
      - backend
    restart: unless-stopped

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:8000:8000"
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/audiobooks.db
      - BOOKS_OUTPUT_DIR=/audiobooks
      - SECRET_KEY=${SECRET_KEY:-local-dev-secret-key}
      # Default local password is "admin123" if ADMIN_PASSWORD_HASH is not set.
      - ADMIN_PASSWORD_HASH=${ADMIN_PASSWORD_HASH:-$$2b$$12$$u4oxHLekoy7LiWDKrFQ7Pu8RaTAeL1/oI27H0b8oXKZV9gOP93zmK}
      - CORS_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000","http://frontend:3000"]
    volumes:
      - ./data:/app/data
      - /tmp/audiobooks:/audiobooks
    restart: unless-stopped
